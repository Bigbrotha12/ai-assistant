name: ai_assistant
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.4.2
  dio: ^5.11.0
  flutter_secure_storage: ^9.2.2
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter_markdown_plus: ^1.0.12
  markdown: ^7.3.1
  url_launcher: ^6.3.2
  uuid: ^4.6.0
  json_schema: ^5.2.2
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  # Attachment picker, local cache directory, and cross-platform file open.
  image_picker: ^1.1.2
  open_file: ^3.5.10
  path_provider: ^2.1.5
  # Persisted app-tier preference (core vs premium). Non-sensitive.
  shared_preferences: ^2.5.5

  # Voice Conversation (Phase 4): mic capture, TTS playback,
  # on-device STT/TTS engines.
  just_audio: ^0.10.6
  record: ^7.1.1
  audio_session: ^0.1.25
  whisper_kit: ^0.3.1
  # On-device TTS runtime (Supertonic 3 int8). Bundles the sherpa-onnx C API
  # and onnxruntime native libs per platform. Pinned to ^1.13.7: the
  # Supertonic vocoder requires onnxruntime >= 1.21, first bundled by 1.13.7.
  sherpa_onnx: ^1.13.7
  # VAD for voice activity detection during mic capture.
  # flutter_vad: ^0.1.2  # — not published on pub.dev; add when available
  # Keep the screen awake while an active voice conversation is running so a
  # long LLM wait / TTS reply does not let the screen fall asleep (the app has
  # no background-audio permission to finish speaking unseen).
  wakelock_plus: ^1.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  http_mock_adapter: ^0.6.1
  freezed: ^4.0.0
  json_serializable: ^6.14.1
  build_runner: ^2.16.0
  drift_dev: ^2.34.3
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that it uses the icons in
  # the material Icons class.
  uses-material-design: true

  # App branding assets (see docs/logo-variants/ for the design review set).
  assets:
    - assets/branding/

  # Design system fonts (see docs/design-system.md). Both are variable fonts;
  # each declared weight maps to the same file so the named weight is applied.
  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-Variable.ttf
          weight: 400
        - asset: assets/fonts/Manrope-Variable.ttf
          weight: 500
        - asset: assets/fonts/Manrope-Variable.ttf
          weight: 600
        - asset: assets/fonts/Manrope-Variable.ttf
          weight: 700
        - asset: assets/fonts/Manrope-Variable.ttf
          weight: 800
    - family: EBGaramond
      fonts:
        - asset: assets/fonts/EBGaramond-Variable.ttf
          weight: 400
        - asset: assets/fonts/EBGaramond-Variable.ttf
          weight: 500
        - asset: assets/fonts/EBGaramond-Variable.ttf
          weight: 600
        - asset: assets/fonts/EBGaramond-Variable.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App launcher icons — generated from the selected logo (Variant C "Echo",
# see docs/logo-variants/). Regenerate with: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
  image_path: "assets/branding/app_logo.png"
  # Variant C ivory/gold is full-bleed ivory; the tool's default 16% adaptive
  # inset keeps the mark inside Android's safe zone, so the same asset serves
  # as foreground. Background matches the tile's ivory paper.
  adaptive_icon_background: "#F6F2E9"
  adaptive_icon_foreground: "assets/branding/app_logo.png"
  remove_alpha_ios: true
  min_sdk_android: 21
